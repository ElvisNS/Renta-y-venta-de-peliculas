@model IEnumerable<Renta_y_venta_de_peliculas.Web.Models.GenerosPeliculas>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista generos peliculas</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cod_pelicula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cod_genero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cod_pelicula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cod_genero)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
